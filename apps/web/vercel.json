{
  "framework": "vite",
  "buildCommand": "corepack pnpm install --frozen-lockfile && corepack pnpm --filter @deadlock/web build",
  "outputDirectory": "dist",
  "installCommand": "corepack pnpm install --frozen-lockfile"
}
